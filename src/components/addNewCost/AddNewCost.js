import CostForm from "./CostForm";
import "./AddNewCost.css";

const AddNewCost = function addNewCost() {
  return (
    <div className="new-cost">
      <CostForm />
    </div>
  );
};

export default AddNewCost;
